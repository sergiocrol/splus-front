export interface Community {
  id: string;
  name: string;
  description: string;
  logoIdentifier: string;
  avgRating: number;
  totalMemberCount: number;
  discussionCount: number;
  blogCount: number;
}
